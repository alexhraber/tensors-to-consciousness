#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

if [[ ! -x ./target/debug/explorer ]]; then
  cargo build -p explorer >/dev/null
fi

./target/debug/explorer ops git-policy --hook pre-commit
./target/debug/explorer ops bootstrap --quiet
